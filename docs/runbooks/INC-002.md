# INC-002 — ORA-20004 (No active tenant for domain)

## Guidance only — AUTO REPAIR FORBIDDEN

1. Confirm TENANT_DOMAINS has ACTIVE_YN=Y for the Host (normalized).
2. Confirm TENANTS.ACTIVE_YN=Y.
3. Provision only via DEALERHUB_PLATFORM_PKG (platform actor).
4. Never disable TRG_TSET_TENANT / VPD / fake context.
5. Fail closed publicly (WebsiteUnavailable).

## Verification
SET_PUBLIC_TENANT_BY_HOST(host) → expected TENANT_ID; CLEAR_CONTEXT after; homepage shows that dealer only.
